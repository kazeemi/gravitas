kind = "web"
previewPath = "/"
title = "Executive Presence"
version = "1.0.0"
id = "artifacts/ep-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24483

[services.development]
run = "pnpm --filter @workspace/ep-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ep-app", "run", "build" ]
publicDir = "artifacts/ep-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24483"
BASE_PATH = "/"
