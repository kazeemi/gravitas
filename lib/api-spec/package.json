{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "fix-index": "node -e \"const fs=require('fs');fs.writeFileSync('../api-zod/src/index.ts', 'export * from \\\"./generated/api\\\";\\n');\"",
    "codegen": "orval --config ./orval.config.ts && pnpm run fix-index && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
